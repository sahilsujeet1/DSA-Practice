auto it=intervals.begin(); it!=intervals.end(); it++) {
    //     if(next(it) < intervals.end() && (*it)[1] >= (*next(it))[0]) {
    //         (*it)[1] = (*next(it))[1];
    //         intervals.erase(next(it));
    //     }  
    // }